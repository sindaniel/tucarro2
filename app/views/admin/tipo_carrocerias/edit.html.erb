<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Tipo Carroceria <small>/ Agregar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>




<!-- /.row -->
<%  if @tipo.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@tipo.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @tipo.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @tipo, :url => admin_tipo_carroceria_path, :method=>'put' do |f|  %>

        <div class="row">
          <div class="col-md-6">
            <div class="form-group">
              <%= f.label :name, 'Nombre tipo de carroceria' %>
              <%= f.text_field :name, :class =>'form-control'  %>
            </div>
          </div>

        </div>

        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_tipo_carrocerias_path, :class=>'btn btn-warning' %>
    <% end %>

  </div>
</div>









