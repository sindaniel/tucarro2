<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Departamento <small>/ Editar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>

<!-- /.row -->
<%  if @state.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@state.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @state.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @state, :url => admin_state_path, :method=>'put' do |f|  %>

        <div class="form-group">
          <%= f.label :name, 'Nombre del departamento' %>
          <%= f.text_field :name, :class =>'form-control'  %>
        </div>

        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_states_path, :class=>'btn btn-warning' %>
    <% end %>



  </div>
</div>








