<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Marcas <small>/ editar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>




<!-- /.row -->
<%  if @marca.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@marca.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @marca.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @marca, :url => admin_marca_volco_path, :method=>'put' do |f|  %>

        <div class="row">
          <div class="col-md-6">
            <div class="form-group">
              <%= f.label :name, 'Nombre de Marca' %>
              <%= f.text_field :name, :class =>'form-control'  %>
            </div>
          </div>



        </div>




        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_marcas_equipos_humedos_path, :class=>'btn btn-warning' %>
    <% end %>

  </div>
</div>









