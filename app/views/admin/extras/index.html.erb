<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Repuestos <small></small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>

<!-- /.row -->
<% if flash[:notice] %>

    <div class="row">
      <div class="col-lg-12">
        <div class="alert alert-info alert-dismissable">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= flash[:notice] %>
        </div>
      </div>
    </div>


<% end %>

<!-- /.row -->

<div class="row">
  <div class="col-md-12">
    <p>
      <%= link_to 'Nuevo repuesto', new_admin_extra_path,:method=>'get', :class=>'btn  btn-primary' %>
    </p>
  </div>
</div>

<div class="row">
  <div class="col-lg-12">



    <div class="table-responsive">
      <table class="table table-bordered table-hover table-striped">
        <thead>
        <tr>
          <th>Nombre</th>
          <th>Precio</th>
          <th>Tipo</th>
          <th>Marca</th>
           <th>Departamento</th>
          <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        <% @extras.each do |extra| %>
            <tr>


              <td>
                <%= extra.name %>
              </td>
              <td>
                <%= number_to_currency( extra.price) %>
              </td>

              <td>

                <%=  extra.type_extra.name  %>
              </td>

              <td>
                <%= extra.brand_extra.name %>
              </td>

              <td>
                <%= extra.state.name %>
              </td>




              <td>
                <%= link_to 'Editar', edit_admin_extra_path(extra.id), :class=>'btn btn-info' %>
                <%= link_to 'Eliminar', {:controller => 'extras', :action => 'destroy', :id => extra.id}, :data => {:confirm => 'Estas seguro de eliminar este elemento?'}, :method => 'DELETE',  :class=>'btn btn-danger' %>
              </td>
            </tr>
        <% end %>


        </tbody>



      </table>
    </div>



  </div>
</div>
<!-- /.row -->
