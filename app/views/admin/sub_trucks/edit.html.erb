<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Subtipo <small>/ Agregar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>

<!-- /.row -->
<%  if @sub.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@sub.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @sub.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @sub, :url => admin_type_truck_sub_truck_path, :method=>'PUT' do |f|  %>

        <div class="form-group">
          <%= f.label :name, 'Nombre del subtipo' %>
          <%= f.text_field :name, :class =>'form-control'  %>
        </div>

        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_type_truck_sub_trucks_path, :class=>'btn btn-warning' %>
    <% end %>



  </div>
</div>








