<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Ciudad <small>/ Editar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>

<!-- /.row -->
<%  if @city.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@city.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @city.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @city, :url => admin_state_city_path, :method=>'PUT' do |f|  %>

        <div class="form-group">
          <%= f.label :name, 'Nombre de la ciudad' %>
          <%= f.text_field :name, :class =>'form-control'  %>
        </div>

        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_state_cities_path, :class=>'btn btn-warning' %>
    <% end %>



  </div>
</div>








