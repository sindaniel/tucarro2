<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Marcas <small>/ Agregar</small>
    </h1>
    <%= render 'admin/includes/top' %>
  </div>
</div>

<!-- /.row -->
<%  if @brand.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(@brand.errors.count, "Errores") %> tratando de guardar:
      </div>
      <p>
      <ul>
        <% @brand.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
      </p>
    </div>
<% end %>
<!-- /.row -->


<div class="row">
  <div class="col-md-12">



    <%= form_for @brand, :url => admin_brands_truck_index_path, :method=>'post' do |f|  %>

        <div class="form-group">
          <%= f.label :name, 'Nombre de marca' %>
          <%= f.text_field :name, :class =>'form-control'  %>
        </div>

        <%= f.submit 'Guardar', :class=> 'btn btn-info' %>
        <%=  link_to 'Cancelar', admin_brands_truck_index_path, :class=>'btn btn-warning' %>
    <% end %>



  </div>
</div>








